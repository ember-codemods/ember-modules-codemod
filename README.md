To use:

```sh
npm install jscodeshift -g
git clone https://github.com/tomdale/ember-modules-codemod
# in your Ember app's root directory:
jscodeshift -t path/to/ember-modules-codemod/transform.js app/
```